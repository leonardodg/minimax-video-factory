# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Models & weights (large, downloaded by scripts/download_models.sh)
models/
*.safetensors
*.bin
*.pt
*.ckpt

# Generated videos & outputs
downloads/
output/
input/
comfyui/output/

# Environment / secrets
# Every .env variant, not just the two by name: .env.bak-int4 was sitting here
# untracked with a live OPENAI_API_KEY, one `git add -A` away from a public
# repo. .env.example is the template and stays versioned.
.env
.env.*
!.env.example
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
.hf-cache/

# Postgres data (docker volume for knowledge base)
.pgdata/

# Docs site build output (generated by mkdocs build)
site/

# Git worktrees
.worktrees/

.rabbitmq

# Claude Code per-machine permissions. `.claude/commands/` IS versioned (it is
# generated from the code, like .opencode/command/); this one file is not.
.claude/settings.local.json
