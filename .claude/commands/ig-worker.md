---
description: Envia o comando 'start' ao daemon ig-worker (retoma o consumo da fila)
---

<!-- GERADO AUTOMATICAMENTE por scripts/generate_commands.py -->
<!-- Não edite à mão: rode `uv run python scripts/generate_commands.py`. -->

Execute a ferramenta MCP **`mcp__minimax-video-factory__ig_worker_start`**.

Entrada do usuário (tudo depois do comando):
$ARGUMENTS

Instruções obrigatórias:
1. Chame `mcp__minimax-video-factory__ig_worker_start` com esses parâmetros. Se o servidor `minimax-video-factory` não estiver conectado, use `mcp__minimax-video-factory-remote__ig_worker_start` ou `mcp__minimax-video-factory-uv__ig_worker_start`.
2. Reporte o resultado da tool ao usuário.
