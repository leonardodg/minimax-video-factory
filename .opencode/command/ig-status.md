---
description: Mostra o tamanho da fila ig.saved (ready/dead) e quantos consumidores ativos. Uso: /ig-status
---

Execute a ferramenta MCP **`minimax-video-factory_ig_queue_status`** (server `minimax-video-factory`).

Entrada do usuário (tudo depois do comando):
$ARGUMENTS

Instruções obrigatórias:
1. Chame `ig_queue_status` com esses parâmetros. Se a variante default do servidor MCP estiver indisponível, use a variante conectada (`minimax-video-factory-remote` ou `minimax-video-factory-uv`).
2. Reporte o resultado da tool ao usuário.
