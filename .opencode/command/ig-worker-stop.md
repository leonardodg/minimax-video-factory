---
description: Envia o comando 'stop' ao daemon ig-worker (pausa o consumo da fila). Uso: /ig-worker-stop
---

Execute a ferramenta MCP **`minimax-video-factory_ig_worker_stop`** (server `minimax-video-factory`).

Entrada do usuário (tudo depois do comando):
$ARGUMENTS

Instruções obrigatórias:
1. Chame `ig_worker_stop` com esses parâmetros. Se a variante default do servidor MCP estiver indisponível, use a variante conectada (`minimax-video-factory-remote` ou `minimax-video-factory-uv`).
2. Reporte o resultado da tool ao usuário.
