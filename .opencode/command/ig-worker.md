---
description: Envia o comando 'start' ao daemon ig-worker (retoma o consumo da fila). Uso: /ig-worker
---

Execute a ferramenta MCP **`minimax-video-factory_ig_worker_start`** (server `minimax-video-factory`).

Entrada do usuário (tudo depois do comando):
$ARGUMENTS

Instruções obrigatórias:
1. Chame `ig_worker_start` com esses parâmetros. Se a variante default do servidor MCP estiver indisponível, use a variante conectada (`minimax-video-factory-remote` ou `minimax-video-factory-uv`).
2. Reporte o resultado da tool ao usuário.
